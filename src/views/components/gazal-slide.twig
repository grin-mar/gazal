{% set el_slide = component %}

<section class="s-block s-block--default home-slider {{ el_slide.sec_width == 'full' ? 's-block--full-bg' : '' }} {{el_slide.txt-clas}} " dir="rtl">

	<div class="{{ el_slide.sec_width == 'Boxed' ? 'container' : '' }}">

		<salla-slider id="home-main-slider" auto-play="{{ el_slide.TIME1 > 0 ? 'true' : 'false' }}"
